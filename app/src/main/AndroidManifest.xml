<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.co.davideandreazzini.jarealestate">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name="jarealestate.MainActivity"
            android:noHistory="true">
        </activity>
        <activity
            android:name="jarealestate.RootActivity"
            android:noHistory="true"
            android:label="@string/title_activity_root"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="login.googleSignIn" />
        <activity
            android:name="jarealestate.SplashActivity"
            android:noHistory="true"
            android:label="JA real estate">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />>
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

    </application>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>